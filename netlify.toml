# Netlify — Next.js on Netlify (SSR). API lives on HuggingFace Spaces.
[build]
  base = "frontend/"
  command = "npm run build"
  publish = "frontend/.next"

[build.environment]
  NEXT_TELEMETRY_DISABLED = "true"
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
